package com.m7.imkfsdk;import android.app.Application;import com.iflytek.cloud.SpeechConstant;import com.iflytek.cloud.SpeechUtility;import com.tencent.bugly.crashreport.CrashReport;/** * Created by longwei */public class MobileApplication extends Application {    private static MobileApplication mobileApplication;    //客服SDK是否已经初始化过了    public static boolean isKFSDK = false;    @Override    public void onCreate() {        super.onCreate();        mobileApplication = this;        //初始化表情,界面效果需要        new Thread(new Runnable() {            @Override            public void run() {                com.m7.imkfsdk.utils.FaceConversionUtil.getInstace().getFileText(                        MobileApplication.getInstance());            }        }).start();        //初始化科大讯飞,不需使用该服务时注掉        SpeechUtility.createUtility(mobileApplication, SpeechConstant.APPID +"=58198ef6");        CrashReport.initCrashReport(mobileApplication, "900005144", false);    }    public static MobileApplication getInstance() {        return mobileApplication;    }}